import 'package:flutter/material.dart';

class FindDevFestPage extends StatelessWidget {
  static const String routeName = "/findDevFest";

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
